module.exports = {
    tags: ['owner'],
    args: ['628xxx'],
    help: ['unblock'],
    cmd: ['unblock', 'unblok'],
    owner: true,
    exec: async (m, client, { body, msg, args }) => {
        m.reply('under construction')
    }
}